group 'de.hhn.it.pp'
version '1.0-SNAPSHOT'


allprojects {

    wrapper {
        gradleVersion = '6.6.1'
    }

    apply plugin: 'java'

    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }

    repositories {
        google()
        jcenter()
    }
}
